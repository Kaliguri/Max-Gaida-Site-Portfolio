---
description: Short session digest — branch, uncommitted changes, current roadmap phase
---

Give me a tight session digest. Keep it to ~5 lines, no preamble. Gather and report:

1. **Git**: current branch + count of uncommitted files (`git status --porcelain`), and the last commit subject.
2. **Roadmap**: the current phase / next step from the auto-memory (`memory/MEMORY.md` and the active plan note it points to). One line.
3. **Checks**: whether `npm run check` was run green this session — otherwise note it hasn't been. Do NOT run it now unless asked.

Do not start any work. Just the digest, then stop.
