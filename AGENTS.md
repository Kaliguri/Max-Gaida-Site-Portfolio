# Agents

This repo is worked on with Claude Code and Cursor. The canonical guide is [`CLAUDE.md`](CLAUDE.md);
Cursor rules live in [`.cursor/rules/`](.cursor/rules/). Status and roadmap live in the auto-memory
(`memory/MEMORY.md`), not in docs.

Do not add agent co-author trailers to commits — commits are authored as Max Gaida.
See [`CLAUDE.md`](CLAUDE.md) → Conventions and [`.cursor/README.md`](.cursor/README.md).
